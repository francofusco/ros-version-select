# source the "main" setup file of the ROS2 distribution we want to use.
source $HOME/programming/ROS2/ros2_foxy/install/local_setup.bash

# Finally, make sure that every time we open a console, we are greeted with a
# message informing us about the version we are using
echo "Working with ROS2 $ROS_DISTRO"
